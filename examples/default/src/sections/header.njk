<style scoped lang="scss">
  .root:scope {
    display: block;
    width: 100%;
    padding: 10px 20px;
    position: fixed;
    background-color: var(--color-background-inverse);
    color: var(--color-text-inverse);
  }

  nav:scope {
    display: flex;
    justify-content: flex-end;

    ul {
      list-style: none;
      margin: 0;
      display: flex;
    }

    li {
      &:not(first-child) {
        margin-left: 20px;
      }
    }

    a {
      font-family: var(--font-sans);
    }
  }
</style>

<template>
  <div class="root">
    <nav>
      <ul>
        {% for item in theme.config.sections.header.navigation %}
          <li>
            <a href="{{ item.url }}" target="_blank" rel="noreferrer noopener">
              {{ item.text }}
            </a>
          </li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</template>